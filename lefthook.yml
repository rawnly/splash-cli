# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
pre-commit:
  parallel: true
  jobs:
    - run: go fmt {staged_files}
      glob: "*.go"
    - run: shellcheck -S error {staged_files}
      glob: "*.{sh,zsh}"
#
